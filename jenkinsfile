pipeline {
    agent any
    stages {
        stage ('build') {
           steps {
              sh 'package'
           }
        }
        stage ('publish_reports') {
           steps {
              echo 'reports are done'
           }
        }
    }
}
