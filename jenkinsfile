pipeline {
    agent any
    stages {
        stage ('build') {
           steps {
              sh 'mvn install'
           }
        }
        stage ('publish_reports') {
           steps {
              echo 'reports are done'
           }
        }
    }
}
