pipeline {
    agent any
    stages {
        stage ('build') {
           steps {
              sh 'mvn build'
           }
        }
        stage ('publish_reports') {
           steps {
              echo 'reports are done'
           }
        }
    }
}
