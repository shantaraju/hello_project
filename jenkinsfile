pipeline {
    agent any
    stages {
        stage ('build') {
           steps {
              echo 'maven project bulded'
           }
        }
        stage ('publish_reports') {
           steps {
              echo 'reports are done'
           }
        }
    }
}
